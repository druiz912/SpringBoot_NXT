CREATE TABLE IF NOT EXISTS PERSONA (
id_persona int GENERATED BY DEFAULT AS IDENTITY,
user_persona VARCHAR(64),
name VARCHAR(64),
surname VARCHAR(64),
address VARCHAR(64),
email VARCHAR(64),
created_date DATE)